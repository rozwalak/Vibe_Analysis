all_summary.tsv should also contain information from {project}_summary.tsv now is incomplete. Check aDNA_panmap_snakemake to see how it should looks like. 


remove results/errors 

I just want to have list in txt file with project and sample names that was corrupted at some point. 

if project have no errors, the folder "errors" shouldn't be created. 

pydamage log is two times, report only pydamage.log.pydamage

"status" folder should be a subfolder of "logs"
